[core]
  editor = code -w
  pager = less -F -X

# TODO: check if this is a Mac specific config when using Linux. GCM was
# installed with Homebrew.
[credential]
	helper = 
	helper = /usr/local/share/gcm-core/git-credential-manager-core
[credential "https://dev.azure.com"]
	useHttpPath = true
