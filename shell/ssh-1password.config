# This config should be put in the ~/.ssh/config file and is only necessary when
# using Linux (without WSL2).
#
# Might want/need to include the following in your shell rc file:
#   export SSH_AUTH_SOCK=~/.1password/agent.sock

Host *
  IdentityAgent ~/.1password/agent.sock
